import { R, X, lesson, mcq, numeric, short } from './helpers';
import type { Lesson } from '@/lib/types';

/**
 * ROS 2.
 *
 * The ROS2 track: the middleware layer that turns a pile of robot code into a
 * system of cooperating processes with typed interfaces, time handling and
 * introspection. Taught as engineering discipline - why the abstractions exist,
 * where they cost you, and how to debug them - rather than as a command tour.
 *
 * Order: why middleware -> messages, topics and QoS -> services, actions and
 * executors -> TF2 and time -> parameters, lifecycle, launch and real-time ->
 * debugging, recording and testing.
 */
export const ROS2_LESSONS: Lesson[] = [
  lesson({
    id: 'ros-01',
    subject: 'ros2',
    order: 1,
    title: 'Why Middleware Exists: Nodes, Topics and the Robot Graph',
    difficulty: 'intermediate',
    minutes: 50,
    prereqs: ['rob-01', 'api-02'],
    description: 'The engineering problem ROS 2 solves: decomposing a robot into processes with typed interfaces, publishing and subscribing over a discovery-based graph, and why ROS 2 replaced ROS 1 with DDS rather than a central master.',
    why: 'A robot is many programs running at different rates on different hardware, written over years by different people. Without a defined interface layer every integration becomes custom glue code that nobody can debug. Understanding what the middleware buys you - and what it costs - is what makes you able to decide when to use it.',
    objectives: [
      'Explain the decomposition problem middleware solves and what it costs',
      'Define nodes, topics, publishers and subscribers and draw the resulting graph',
      'Compare ROS 2 with ROS 1 and explain why DDS replaced the central master',
      'Decide when a robot project should use ROS 2 and when it should not',
      'Design a node decomposition for a robot with explicit interfaces',
    ],
    learn: [
      {
        kind: 'table',
        heading: 'What middleware gives you, and what it takes',
        columns: ['You gain', 'You pay'],
        rows: [
          ['Decomposition: independent processes, each testable and restartable alone', 'Inter-process communication overhead and latency you must budget for'],
          ['Typed interfaces: a message definition is a contract between authors', 'You must learn and maintain the type system; bad types spread through the graph'],
          ['Discovery: nodes find each other without hardcoded addresses', 'Non-deterministic startup ordering; you must design for late-arriving data'],
          ['Introspection: any topic can be echoed, plotted, recorded and replayed', 'Everything on the network is visible - a security and bandwidth consideration'],
          ['Reuse: drivers, planners, localizers written by others', 'Your architecture must match theirs, or you end up writing adapters forever'],
          ['Tooling: bags, visualisation, launch, lifecycle management', 'A large dependency surface and a steep learning curve before the first working robot'],
        ],
      },
      {
        kind: 'text',
        heading: 'The four primitives',
        body: [
          'A NODE is a process (or a component inside one) that does one job: read a lidar, run a controller, plan a path. Nodes should be independently restartable and should not share memory implicitly.',
          'A TOPIC is a named, typed, many-to-many channel for continuous data flow - sensor readings, velocity commands, transforms. Publish and subscribe, with no knowledge of who is on the other end.',
          'A MESSAGE is a typed record defined in an .msg file (or a standard type such as sensor_msgs/LaserScan). Types are the contract: changing a message definition is an API change and must be versioned.',
          'The GRAPH is the set of nodes and their topic connections. Discovery (in ROS 2, via DDS) builds it at runtime, which is why you can inspect a running system without modifying it.',
        ],
      },
      {
        kind: 'callout',
        tone: 'note',
        heading: 'Why ROS 2 replaced ROS 1',
        body: [
          'ROS 1 routed everything through a central master and used a custom TCP-based transport. That gave a single point of failure, no real-time guarantees, poor behaviour across wireless links, and no security model.',
          'ROS 2 puts DDS (the Data Distribution Service, an industry standard used in defence, aerospace and finance) underneath. Consequences: decentralised discovery with no master, configurable Quality of Service per topic, real-time-capable transports, security via DDS-Security, and the possibility of running across multiple machines or shared memory within one.',
          'The trade you accept: DDS configuration is more complex, some ROS 1 packages had to be ported, and the extra abstraction can cost latency if QoS and threading are set carelessly.',
        ],
      },
      {
        kind: 'example',
        heading: 'Worked example - decomposing a two-wheeled delivery robot',
        problem: 'Decompose a differential-drive robot with a lidar, a camera, wheel encoders and a mission into nodes and topics, at the loop rates from the robotics architecture lesson.',
        solution: [
          'motor_driver node (1 kHz loop): subscribes /cmd_vel (geometry_msgs/Twist), publishes /odom (nav_msgs/Odometry) and /joint_states; owns the PID cascades and safety limits',
          'lidar_driver node (10-20 Hz): publishes /scan (sensor_msgs/LaserScan)',
          'camera_driver node (15-30 Hz): publishes /camera/image_raw or a compressed topic',
          'localization node (50 Hz): subscribes /scan and /odom, publishes /tf (map->odom) and a pose with covariance',
          'planner node (1-5 Hz): subscribes map and goal, publishes a global path on /plan',
          'controller/avoidance node (20-50 Hz): subscribes /plan, /scan, /odom, publishes /cmd_vel',
          'mission node (0.1-1 Hz): a state machine publishing goals to the planner via an action',
          'Interface discipline: only the motor_driver publishes odometry, and only the controller publishes /cmd_vel - single-writer topics prevent command conflicts',
          'Fallback: if the mission node dies, the controller still avoids obstacles; if localization dies, the controller stops because /tf goes stale - which is a designed behaviour, not an accident',
        ],
        answer: 'Seven nodes with one writer per topic. The safety property comes from the interface design: fast nodes own motion, slow nodes own intent, and stale inputs cause a defined stop rather than blind continuation.',
      },
    ],
    resources: [
      R('ROS 2 documentation: concepts and design articles', 'docs', 'https://docs.ros.org/en/rolling/Concepts/Basic/About-ROS-2.html', { author: 'Open Robotics', minutes: 90, note: 'Free and authoritative. Read "About ROS 2" and the concepts on nodes, topics and DDS before writing any code.' }),
      R('ROS 2 design articles (why ROS 2 exists)', 'docs', 'https://design.ros2.org/articles/why_ros_2.html', { author: 'Open Robotics', minutes: 30, note: 'The explicit rationale: real-time, security, multi-robot, production use. Short and worth reading in full.' }),
      R('Wikipedia: Data Distribution Service', 'article', 'https://en.wikipedia.org/wiki/Data_Distribution_Service', { minutes: 30, note: 'The standard underneath ROS 2, including its origin in defence and finance - which explains why its QoS model is so detailed.' }),
      R('ROS 2 tutorials: creating a package and writing a publisher/subscriber', 'docs', 'https://docs.ros.org/en/rolling/Tutorials.html', { author: 'Open Robotics', minutes: 180, note: 'Free, step by step, in Python and C++. Do the client-library tutorials on a real install.' }),
    ],
    exercises: [
      X('question', 'Draw the node and topic graph for a robot you intend to build: every node, every topic with its message type, the rate of each topic, and which node is the single writer. Mark which topics are safety-critical.', 40, { solution: 'A complete answer has 5-10 nodes, message types named from the standard packages (sensor_msgs/LaserScan, nav_msgs/Odometry, geometry_msgs/Twist), rates within the cascade from the robotics lesson, exactly one writer per topic, and a marked safety-critical set (/cmd_vel, /odom, /scan, /tf).' }),
      X('question', 'List three concrete costs of adopting ROS 2 for a small single-board robot, and state the alternative you would use instead for each if you decided not to adopt it.', 30, { solution: 'Realistic costs: CPU and memory overhead on a constrained board, latency and jitter from inter-process transport, and the learning/dependency burden. Alternatives: a single-process design with direct function calls and a message struct, a lightweight pub/sub library, or a hand-rolled shared-memory bus. The point is that the alternative must be named, not just criticised.' }),
      X('code', 'Install ROS 2 and run a two-node demo: a publisher sending a counter at 10 Hz and a subscriber printing it. Then use the CLI to list nodes, echo the topic and measure its actual rate. Report the measured rate against the configured rate.', 60, { solution: 'ros2 run / ros2 topic list / ros2 topic echo / ros2 topic hz. The measured rate should be close to 10 Hz; any systematic shortfall reveals timer resolution or executor scheduling issues, which is exactly what the next lessons dig into.' }),
    ],
    questions: [
      mcq(1, 'What is the core problem ROS-style middleware solves?', ['Decomposing a robot into independent processes with typed interfaces so they can be developed, tested and restarted separately', 'Making robots faster', 'Replacing real-time control', 'Providing machine learning models'], 0, 'It is a software architecture tool. The gains are decomposition, typed contracts, discovery and introspection - and each comes with a cost in latency, complexity and dependencies.'),
      mcq(1, 'Which is NOT something you pay for when adopting middleware?', ['Deterministic startup ordering of all nodes', 'Inter-process communication overhead', 'A large dependency surface', 'Learning the type system'], 0, 'Discovery-based systems have the opposite property: startup order is not deterministic, so your nodes must tolerate missing or late data. That is a design requirement, not a benefit.'),
      mcq(2, 'A topic in ROS 2 is:', ['A named, typed, many-to-many channel for continuous data flow', 'A one-to-one function call', 'A file on disk', 'A shared memory pointer'], 0, 'Topics suit streams. Request/response belongs to services, and long-running tasks with feedback belong to actions.'),
      mcq(2, 'What is a node?', ['A process or component that performs one job and communicates through topics, services and actions', 'A message type', 'A network cable', 'A topic endpoint'], 0, 'One job per node, restartable independently, with no implicit shared state. That discipline is what makes the graph debuggable.'),
      mcq(2, 'Changing a message definition is:', ['An interface change that must be versioned, because every publisher and subscriber depends on it', 'A private implementation detail', 'Always backwards compatible', 'Only relevant in C++'], 0, 'Message types are the contract between independently written nodes. Silent changes break consumers at runtime, often with confusing deserialisation errors.'),
      mcq(3, 'Why does ROS 2 use DDS instead of a central master?', ['Decentralised discovery removes the single point of failure and brings configurable QoS, real-time capability and security', 'DDS is faster in every case', 'The master was too slow to write', 'DDS avoids the need for message types'], 0, 'ROS 1\'s master was a single point of failure with no QoS and no security model. DDS is an industry standard from defence and finance, which is why its QoS vocabulary is so detailed.'),
      mcq(3, 'What is a real trade-off you accept with ROS 2?', ['DDS configuration is more complex and careless QoS or threading choices can cost latency', 'No introspection', 'No multi-machine support', 'No security model'], 0, 'The abstractions are powerful but not free. QoS mismatches silently drop connections, and default threading can add jitter to a control loop.'),
      mcq(4, 'Which robot project is the weakest candidate for ROS 2?', ['A single-board line-following robot with one sensor and one control loop', 'A mobile robot with lidar, camera and a navigation stack', 'A multi-computer robot with distributed sensing', 'A research arm using existing planners'], 0, 'Middleware pays off when you have multiple processes, heterogeneous hardware, or reuse of existing packages. A tiny single-loop robot gains overhead and loses determinism.'),
      mcq(4, 'What is the strongest reason to adopt ROS 2 despite its cost?', ['Reuse of drivers, planners and localizers, plus tooling for recording and replay', 'It makes control loops faster', 'It removes the need for tests', 'It guarantees real-time behaviour'], 0, 'The ecosystem is the value: you get a navigation stack, transform handling and bag recording rather than writing them. Real-time still requires deliberate engineering on top.'),
      mcq(5, 'Two nodes both publish /cmd_vel. What is the risk and the design rule?', ['Their commands interleave unpredictably; the rule is one writer per topic', 'Nothing - last message wins safely', 'The graph rejects it automatically', 'QoS resolves the conflict'], 0, 'Multiple writers on a command topic produce non-deterministic behaviour that is very hard to diagnose. Enforce single-writer topics, especially for anything that moves the robot.'),
      numeric(5, 'A node decomposition has a motor driver at 1 kHz, a controller at 50 Hz and a planner at 2 Hz. How many times faster is the motor loop than the planner? [0-5000]', 500, '1000/2 = 500. Each cascade level should be roughly 4-20x faster than the one outside it, and the outermost level must never be required for safety.', { tolerance: 1 }),
      short(5, 'Why must a ROS 2 node tolerate data that arrives late or never arrives at all?', ['discovery makes startup order nondeterministic so nodes cannot assume peers exist', 'startup ordering is not deterministic', 'peers may not exist yet'], 'Discovery-based systems have no fixed startup order and peers can crash, restart or be on another machine. A node that blocks waiting for a topic that has not appeared yet deadlocks the whole system, so design for absence: time out, degrade, and report.'),
    ],
    skills: ['ros2-graph', 'node-decomposition', 'dds-middleware', 'single-writer-topics'],
  }),

  lesson({
    id: 'ros-02',
    subject: 'ros2',
    order: 2,
    title: 'Messages, Topics and Quality of Service',
    difficulty: 'intermediate',
    minutes: 55,
    prereqs: ['ros-01'],
    description: 'Standard message types and how to choose or define your own, then QoS: reliability, durability, history, depth and deadlines - the settings that decide whether two nodes actually connect and what happens under load.',
    why: 'Most "my topic is not working" bugs in ROS 2 are QoS incompatibilities, and most bandwidth problems are message-design mistakes. Getting the type right and the QoS deliberate turns a fragile graph into one whose behaviour under load you can predict.',
    objectives: [
      'Use the standard message packages correctly and choose between them and a custom type',
      'Explain header, timestamp and frame_id discipline in every message',
      'Configure reliability, durability, history and depth and explain each trade-off',
      'Diagnose an incompatible-QoS connection failure',
      'Compute the bandwidth of a topic and decide how to reduce it',
    ],
    learn: [
      {
        kind: 'table',
        heading: 'The standard messages you will use constantly',
        columns: ['Type', 'Package', 'Used for'],
        rows: [
          ['Twist', 'geometry_msgs', 'Linear and angular velocity commands (/cmd_vel) - no position, no frame beyond convention'],
          ['Pose / PoseStamped / PoseWithCovariance', 'geometry_msgs', 'Positions and orientations, optionally stamped and with uncertainty'],
          ['Transform / TransformStamped', 'geometry_msgs', 'Relative poses between frames - the payload of TF2'],
          ['LaserScan', 'sensor_msgs', '2D lidar: angle min/max/increment plus a range array'],
          ['Image / CompressedImage', 'sensor_msgs', 'Camera frames; the compressed variant is usually mandatory over wifi'],
          ['PointCloud2', 'sensor_msgs', '3D point sets with per-point fields; large, so handle with care'],
          ['Imu', 'sensor_msgs', 'Orientation, angular velocity and linear acceleration with covariances'],
          ['Odometry', 'nav_msgs', 'Pose plus twist plus covariances - the standard odometry message'],
          ['OccupancyGrid', 'nav_msgs', 'A costmap or occupancy grid with resolution and origin'],
          ['JointState', 'sensor_msgs', 'Names, positions, velocities and efforts for every joint'],
        ],
      },
      {
        kind: 'callout',
        tone: 'warning',
        heading: 'Timestamps and frame_id are not decoration',
        body: [
          'Almost every useful message carries a std_msgs/Header with a stamp and a frame_id. Both are load-bearing: the stamp is what lets TF2 interpolate a transform for the moment the sensor actually measured, and frame_id says which frame the numbers are in.',
          'Two rules that prevent whole categories of bug. First: stamp sensor data with the time of MEASUREMENT, not the time you published it - the difference is your processing latency and it matters when the robot is moving. Second: always fill frame_id, even when it feels obvious; an empty frame_id is the cause of silent, confusing transform failures.',
          'Use the same clock everywhere. Mixing system time on one machine and a simulated clock on another makes every timestamp comparison meaningless - which is why ROS 2 has a /clock topic and a use_sim_time parameter.',
        ],
      },
      {
        kind: 'table',
        heading: 'QoS policies that actually change behaviour',
        columns: ['Policy', 'Options', 'When it bites you'],
        rows: [
          ['Reliability', 'RELIABLE (retransmit) or BEST_EFFORT (drop)', 'A reliable subscriber will not connect to a best-effort publisher - the classic silent failure'],
          ['Durability', 'VOLATILE (only new messages) or TRANSIENT_LOCAL (late joiners get the last message)', 'Maps and static transforms need TRANSIENT_LOCAL so a node starting later still receives them'],
          ['History + depth', 'KEEP_LAST(n) or KEEP_ALL, with a queue depth', 'Too small a depth drops messages under bursts; too large adds latency by queuing stale data'],
          ['Deadline / lifespan', 'Expected publish period; message expiry', 'Lets a consumer detect a dead publisher instead of waiting forever on stale data'],
          ['Liveliness', 'Automatic or by-topic lease', 'Detects a crashed publisher so the subscriber can fall back'],
        ],
      },
      {
        kind: 'formula',
        heading: 'Bandwidth arithmetic you should be able to do in your head',
        formula: 'bytes_per_second = message_size * rate     for raw RGB: width * height * 3 * fps',
        defines: ['message_size = serialised payload, including array elements'],
        body: [
          'A 640x480 RGB image at 30 fps is 640*480*3*30 = 27.6 MB/s, which is about 221 Mbps - more than most wifi links carry reliably, and far more than a small board can move between processes comfortably.',
          'The fixes, in order of preference: publish compressed images (JPEG typically reduces this by 10-20x), reduce resolution or rate to what the algorithm actually needs, process on the camera-side node and publish only results (detections, not pixels), or move the data over shared memory rather than the network.',
          'Do this arithmetic for every high-rate topic before you run the robot. Bandwidth problems look like control problems: the loop stutters, the network saturates, and the robot behaves erratically.',
        ],
      },
      {
        kind: 'example',
        heading: 'Worked example - diagnosing a topic that will not connect',
        problem: 'A lidar driver publishes /scan and your obstacle node subscribes to /scan. `ros2 topic list` shows the topic, `ros2 topic info /scan` shows 1 publisher and 1 subscription, but the subscriber never receives data. Diagnose.',
        solution: [
          'The topic exists and both endpoints are discovered, so this is not a naming or discovery problem',
          'Check QoS compatibility: `ros2 topic info /scan --verbose` shows the reliability and durability of each endpoint',
          'Lidar drivers commonly publish BEST_EFFORT (high rate, dropping a scan is acceptable); a subscriber created with the default reliable profile will NOT connect',
          'That incompatibility is reported by DDS as a QoS mismatch and produces exactly this symptom: endpoints visible, zero messages delivered',
          'Fix: subscribe with a best-effort (or sensor-data) QoS profile - in ROS 2 the rclpy/qos_profile_sensor_data preset does this',
          'Verify by measuring the rate on the receiving side rather than assuming: `ros2 topic hz /scan` from a third process, then a callback counter inside your node',
          'Design rule: sensor streams get sensor-data QoS (best effort, keep last 5-10); commands and maps get reliable; latched one-shot data such as a map gets transient local',
        ],
        answer: 'QoS incompatibility: a reliable subscriber cannot connect to a best-effort publisher. Use the sensor-data profile for streams, reliable for commands, transient-local for latched data - and confirm with a measured rate rather than an assumption.',
      },
    ],
    resources: [
      R('ROS 2 documentation: about Quality of Service settings', 'docs', 'https://docs.ros.org/en/rolling/Concepts/Basic/About-Quality-of-Service-Settings.html', { author: 'Open Robotics', minutes: 45, note: 'Free and the definitive reference on which QoS combinations are compatible.' }),
      R('ROS 2 common message packages (sensor_msgs, geometry_msgs, nav_msgs)', 'docs', 'https://docs.ros.org/en/rolling/p/sensor_msgs/', { author: 'Open Robotics', minutes: 60, note: 'Read the field definitions of LaserScan, Image, Imu and PointCloud2 once - the units and conventions are spelled out there.' }),
      R('ROS 2 documentation: topics tutorial', 'docs', 'https://docs.ros.org/en/rolling/Tutorials/Beginner-CLI-Tools/Understanding-ROS2-Topics/Understanding-ROS2-Topics.html', { author: 'Open Robotics', minutes: 40 }),
    ],
    exercises: [
      X('calculation', 'Compute the bandwidth of each topic on your robot graph: raw 640x480 RGB at 30 fps, a 360-point lidar scan at 15 Hz, odometry at 50 Hz, and a 100x100 cell occupancy grid published at 1 Hz. Total them and compare with a 50 Mbps wifi link. State which topics you would change and how.', 35, { solution: 'Image 27.6 MB/s (221 Mbps) - already over the link alone. LaserScan 360*4 bytes*15 = 21.6 kB/s. Odometry roughly 200 bytes*50 = 10 kB/s. Grid 10,000 bytes*1 = 10 kB/s. Fix the image: compress (about 1-2 MB/s), or publish detections instead of pixels. Everything else is negligible.' }),
      X('code', 'Write a publisher and subscriber for a sensor stream with deliberately mismatched QoS, confirm no data flows, then fix the profiles and confirm data flows. Log both `ros2 topic info --verbose` outputs as evidence.', 45, { solution: 'The demonstration is the point: reliable subscriber plus best-effort publisher gives 1 publisher, 1 subscription, zero messages. Switching the subscriber to sensor-data QoS fixes it instantly. Save the verbose output - it is the diagnostic you will need later.' }),
      X('question', 'For each of these, specify the QoS profile and justify it: a map published once at startup, a lidar stream at 20 Hz, velocity commands at 50 Hz, and a diagnostic message that must never be lost but may arrive late.', 30, { solution: 'Map: transient local, reliable, keep last 1 - late joiners must still get it. Lidar: best effort, keep last 5-10 - dropping a scan is fine, stale scans are not. Velocity commands: reliable, keep last 1 - you want the newest command and no loss, but a queue of old commands would be dangerous. Diagnostics: reliable with keep all and a generous depth - loss matters more than latency.' }),
    ],
    questions: [
      mcq(1, 'Which message type is the standard for a velocity command?', ['geometry_msgs/Twist', 'nav_msgs/Odometry', 'sensor_msgs/JointState', 'geometry_msgs/Pose'], 0, 'Twist carries linear and angular velocity with no position. Odometry is pose plus twist plus covariance - a different job.'),
      mcq(1, 'When should you define a custom message type instead of using a standard one?', ['When no standard type expresses your data without abuse - otherwise reuse, because the ecosystem tools and packages understand standard types', 'Always, for control', 'When the standard type is large', 'Never'], 0, 'Standard types buy interoperability with existing planners, visualisers and bags. Custom types are for genuinely new data, and should still carry a header.'),
      mcq(2, 'What does a message header provide?', ['A timestamp and a frame_id, so data can be time-synchronised and transformed', 'Only a sequence number', 'QoS settings', 'The message type name'], 0, 'The stamp enables TF2 lookup at the moment of measurement; frame_id says which frame the numbers live in. Both are required for any spatial data.'),
      mcq(2, 'A lidar message should be stamped with:', ['The time the measurement was taken, not the time it was published', 'The publish time', 'Zero', 'The time the driver started'], 0, 'Publish time includes processing latency, so the robot would transform the scan using a pose from later than the measurement - producing a smeared or shifted obstacle map when moving.'),
      mcq(3, 'A reliable subscriber and a best-effort publisher on the same topic will:', ['Not connect: the QoS is incompatible and no messages are delivered', 'Connect with warnings', 'Connect and drop half the messages', 'Negotiate to reliable'], 0, 'This is the most common ROS 2 beginner failure. `ros2 topic info --verbose` shows each endpoint\'s QoS; use the sensor-data profile for streams.'),
      mcq(3, 'Which durability is right for a map published once at startup that later nodes need?', ['TRANSIENT_LOCAL, so late-joining subscribers receive the last published message', 'VOLATILE', 'BEST_EFFORT', 'KEEP_ALL'], 0, 'Volatile delivery reaches only those already subscribed. Maps, static transforms and latched configuration need transient local - the ROS 2 equivalent of ROS 1 latching.'),
      mcq(3, 'A queue depth of 100 on a 50 Hz velocity command topic causes what problem?', ['Latency: the consumer may act on commands up to two seconds old, which is dangerous for motion', 'Message loss', 'Higher bandwidth', 'QoS incompatibility'], 0, 'For commands, keep_last(1) is usually correct: you want the newest intent, and a backlog of stale commands makes the robot act in the past.'),
      mcq(4, 'The first diagnostic step when a topic delivers no data is:', ['Check QoS compatibility and endpoint counts with `ros2 topic info --verbose`', 'Rewrite the node', 'Increase the queue depth', 'Restart the DDS daemon only'], 0, 'Visible endpoints plus zero messages almost always means a QoS mismatch or a publisher that never actually publishes. Measure before changing code.'),
      mcq(4, 'A publisher is running but `ros2 topic hz` reports a lower rate than configured. What is a likely cause?', ['The publishing loop is slower than the timer period - blocking work in the callback, or an executor that cannot service the timer in time', 'QoS durability', 'The topic name is wrong', 'The message type is too small'], 0, 'Callbacks run in executor threads. Blocking work or a single-threaded executor with heavy callbacks starves the timer, and the symptom looks like a QoS problem but is a threading one.'),
      numeric(5, 'What is the bandwidth in MB/s of a raw 320x240 RGB image at 15 fps? [0-100]', 3.46, '320*240*3 = 230,400 bytes per frame; times 15 = 3.456 MB/s (about 27.6 Mbps). Halving each dimension would quarter it.', { tolerance: 0.05 }),
      numeric(5, 'A 720-point lidar scan publishes float32 ranges at 20 Hz. What is the bandwidth in kB/s? [0-1000]', 57.6, '720*4 bytes = 2880 bytes per scan; times 20 = 57,600 B/s = 57.6 kB/s - negligible next to an image, which is why lidar is the preferred navigation sensor on constrained hardware.', { tolerance: 0.5 }),
      short(5, 'Why is publishing raw camera images over wifi usually a design mistake?', ['bandwidth saturates the link and starves control topics, so publish compressed data or results instead', 'it saturates the link and starves other topics', 'control data competes with pixel data'], 'The bandwidth (hundreds of Mbps) saturates the link, so control and odometry messages queue behind pixels - producing stutter that looks like a control bug. Publish compressed images, reduce resolution and rate to what the algorithm needs, or process on the sensor node and publish only results.'),
    ],
    skills: ['ros2-messages', 'qos-profiles', 'timestamp-discipline', 'bandwidth-budgeting'],
  }),

  lesson({
    id: 'ros-03',
    subject: 'ros2',
    order: 3,
    title: 'Services, Actions and Executors',
    difficulty: 'intermediate',
    minutes: 55,
    prereqs: ['ros-02'],
    description: 'Request/response services and when they are the wrong tool, action interfaces for long-running goals with feedback and cancellation, and the executor and threading model that decides whether your callbacks ever run.',
    why: 'Choosing the wrong communication pattern is a design error that shows up as a frozen robot: a service call blocking a control loop, or a goal that cannot be cancelled. Understanding actions and the executor model is what lets a slow task and a fast loop coexist safely.',
    objectives: [
      'Choose between topics, services and actions for a given interaction',
      'Implement a service server and client and explain why services block',
      'Use the action interface: goal acceptance, feedback, result and cancellation',
      'Explain the executor model and how callback groups affect concurrency',
      'Diagnose a robot that freezes because a slow callback starves a fast one',
    ],
    learn: [
      {
        kind: 'table',
        heading: 'Which pattern for which interaction',
        columns: ['Interaction', 'Pattern', 'Why'],
        rows: [
          ['Continuous sensor stream or command', 'Topic', 'One-to-many, no response expected, rate-driven'],
          ['Quick question with a definite answer (get a map, set a mode, calibrate)', 'Service', 'Request/response; must complete fast or you have chosen wrong'],
          ['Long-running task (navigate to a point, pick an object, run a calibration sweep)', 'Action', 'Goal accepted or rejected, periodic feedback, cancellable, terminal result'],
          ['Broadcasting state everyone needs', 'Topic (often transient local)', 'Many consumers, no coordination needed'],
          ['Anything that might take longer than a control period', 'Never a service on a control node', 'A blocking call in a fast loop stalls the loop'],
        ],
      },
      {
        kind: 'text',
        heading: 'Actions: the goal lifecycle',
        body: [
          'An action is three services plus two topics packaged into one interface: the client sends a goal, the server accepts or rejects it, the server publishes feedback periodically, the client may request cancellation, and the server returns a result with a status.',
          'That lifecycle is why actions are the right pattern for robot tasks. Acceptance lets the server refuse an impossible goal (unreachable waypoint, arm not homed) instead of silently failing later. Feedback lets a UI show progress and lets a supervisor detect a stuck task. Cancellation is a safety feature: the operator or a higher layer must be able to stop a motion in progress.',
          'Design rules that matter in practice: publish feedback at a fixed, modest rate (1-5 Hz is plenty); make cancellation cooperative and prompt - actually stop the motion, do not just set a flag; and always return a terminal result, because a client waiting forever is a hung system.',
        ],
      },
      {
        kind: 'callout',
        tone: 'warning',
        heading: 'Executors and the freeze everyone hits once',
        body: [
          'Callbacks do not run by magic: an executor pulls work (timer expiries, received messages, service requests) and calls your functions. A single-threaded executor runs one callback at a time, so a callback that blocks for 3 seconds stops every other callback in that node for 3 seconds.',
          'The symptom is unmistakable: your 50 Hz control loop stops publishing while a service call or a long computation runs. The robot drifts, or a watchdog fires, or nothing happens at all.',
          'The fixes: keep callbacks short and hand heavy work to a worker thread; use callback groups (Reentrant or MutuallyExclusive) with a MultiThreadedExecutor so unrelated callbacks can run concurrently; put blocking or slow work in a separate node/process entirely; and never make a synchronous service call from inside a fast timer callback.',
          'Whatever you choose, the design goal is the same: no callback may delay a safety-relevant callback. That is a scheduling requirement, and it must be tested under load, not assumed.',
        ],
      },
      {
        kind: 'example',
        heading: 'Worked example - a navigation action done properly',
        problem: 'Implement "navigate to pose" for a mobile robot using an action, with feedback, cancellation and failure handling.',
        solution: [
          'Goal: a target PoseStamped plus a tolerance. Server validates it: is it inside the map, is it in free space, is localization healthy? Reject with a reason if not - rejection is cheaper than a doomed run',
          'On acceptance, the server starts the task and publishes feedback at 2 Hz: distance remaining, current pose, estimated time, planner status, and a health flag',
          'Cancellation: set a stop flag, command zero velocity immediately, wait for the robot to actually halt (verify with odometry), then return a CANCELED result',
          'Failure paths return explicit results: stuck (no progress for N seconds), localization lost, planner failed, obstacle blocked the goal. Each is a distinct status so the mission layer can react differently',
          'Success returns the final pose and the achieved tolerance - which the mission layer should verify rather than trust',
          'The controller node keeps running at 50 Hz in its own process throughout; the action server never issues motor commands directly, so a slow action cannot stall motion control',
        ],
        answer: 'Validate and reject bad goals, publish modest-rate feedback, make cancellation cooperative and verified by odometry, return distinct terminal results for each failure mode, and keep the action server out of the fast control process.',
      },
    ],
    resources: [
      R('ROS 2 documentation: about actions', 'docs', 'https://docs.ros.org/en/rolling/Concepts/Basic/About-Actions.html', { author: 'Open Robotics', minutes: 30, note: 'Free. The goal/feedback/result/cancel lifecycle, with the underlying services and topics listed.' }),
      R('ROS 2 documentation: about services', 'docs', 'https://docs.ros.org/en/rolling/Concepts/Basic/About-Services.html', { author: 'Open Robotics', minutes: 25 }),
      R('ROS 2 documentation: about executors and callback groups', 'docs', 'https://docs.ros.org/en/rolling/Concepts/Basic/About-Executors.html', { author: 'Open Robotics', minutes: 45, note: 'Threading, callback groups and the consequences for latency. Read before you write a node with more than one callback.' }),
    ],
    exercises: [
      X('code', 'Implement a service server that returns the current battery voltage and a client that calls it. Then deliberately block the server callback for 2 seconds and measure what happens to a 20 Hz timer callback in the same node. Report the measured effect.', 50, { solution: 'The timer callback stalls for the duration of the blocking call - with a single-threaded executor the measured rate collapses. Fix with a MultiThreadedExecutor plus a Reentrant callback group, or move the blocking work to a thread, and re-measure to confirm the timer holds its rate.' }),
      X('code', 'Implement a long-running action (for example "drive a square" or "sweep the arm through N poses") with goal validation and rejection, 2 Hz feedback, cooperative cancellation verified by reading the sensor, and distinct results for success, cancellation and failure.', 75, { solution: 'The four behaviours to demonstrate: a rejected invalid goal, feedback arriving at the configured rate, a cancellation that actually halts motion (proved by a sensor reading, not a flag), and distinct terminal statuses. Test cancellation mid-motion, not after completion.' }),
      X('question', 'Your robot freezes for half a second every time a new map is loaded, and the control loop drops frames. Explain three possible causes in terms of the executor model and give a fix and a measurement for each.', 35, { solution: '(1) Map deserialisation in a subscription callback on the same executor as the control timer - fix by moving it to a separate node or thread, measure with ros2 topic hz on the control topic. (2) A transient-local map message with a large depth causing a burst of callbacks - fix by reducing depth, measure callback count. (3) A single-threaded executor serving a heavy planner - fix with a MultiThreadedExecutor and callback groups, measure the control loop jitter before and after.' }),
    ],
    questions: [
      mcq(1, 'Which pattern should a "navigate to this pose" task use?', ['An action: it is long-running, needs feedback, and must be cancellable', 'A service', 'A topic', 'A parameter'], 0, 'Services must complete quickly and cannot report progress or be cancelled. A navigation task needs goal acceptance, periodic feedback and prompt cancellation.'),
      mcq(1, 'Which is the right use of a service?', ['A quick request with a definite answer, such as getting a map or setting a mode', 'Streaming sensor data', 'A task taking ten seconds', 'Broadcasting state'], 0, 'If the server cannot answer promptly, it blocks the client and possibly the server\'s own executor. Long work belongs in an action.'),
      mcq(2, 'Why is a synchronous service call from inside a fast timer callback dangerous?', ['It blocks the executor thread, so the timer callback cannot run again until the call returns', 'It corrupts the message', 'It changes the QoS', 'It is not - services are asynchronous'], 0, 'With a single-threaded executor one blocked callback stops every callback in that node. Never block a loop that moves the robot.'),
      mcq(2, 'What should a service server do if it cannot answer quickly?', ['Return an error or an "in progress" response and move the work to an action or a worker thread', 'Block until finished', 'Drop the request silently', 'Increase the queue depth'], 0, 'A prompt, honest failure is far better than a silent block. Redesign the interface as an action if the work is inherently slow.'),
      mcq(3, 'An action server receives an impossible goal. What is the correct behaviour?', ['Reject the goal with a reason, before starting any motion', 'Accept and fail later', 'Accept and silently do nothing', 'Cancel the previous goal and accept'], 0, 'Rejection at acceptance time is cheap and safe. Accepting a goal you cannot achieve produces a robot that appears to work and then fails mid-motion.'),
      mcq(3, 'What makes action cancellation "cooperative"?', ['The server actually stops the motion and verifies it stopped, rather than only setting a flag', 'The client cancels twice', 'The server ignores feedback', 'Cancellation is automatic in DDS'], 0, 'A cancel request must result in zero velocity and a verified halt - confirm with odometry or encoders. A flag that no one checks is not cancellation.'),
      mcq(3, 'Feedback should be published at:', ['A modest fixed rate such as 1-5 Hz - enough for a UI and for stuck-detection, not enough to load the network', 'The maximum possible rate', 'Once at the end', 'Only on request'], 0, 'Feedback is for progress and health monitoring. High rates add bandwidth and jitter for no benefit; too low and you cannot detect a stuck task promptly.'),
      mcq(4, 'In a single-threaded executor, two callbacks are ready at the same time. What happens?', ['One runs to completion, then the other - they never run concurrently', 'Both run in parallel', 'The older one is dropped', 'The executor spawns a thread'], 0, 'Serialisation is the whole point of a single-threaded executor: predictable, but vulnerable to any slow callback. Concurrency requires a MultiThreadedExecutor with callback groups.'),
      mcq(4, 'A MutuallyExclusive callback group means:', ['Callbacks in that group never run concurrently with each other, which is what you want for shared state', 'All callbacks in the node are serialised', 'The group is disabled', 'Callbacks run in a separate process'], 0, 'Use mutually exclusive groups to protect shared state and reentrant groups where concurrency is safe. Grouping is how you design the threading instead of inheriting it.'),
      mcq(5, 'Your 50 Hz control topic drops to 2 Hz while a planner runs in the same node. What is the most robust fix?', ['Move the planner to its own process/node, so it cannot starve the control loop', 'Increase the control timer rate', 'Increase the queue depth', 'Reduce planner accuracy only'], 0, 'Process separation gives real isolation: independent scheduling, independent restart, and no shared executor. Threading tweaks help, but the control loop belongs in its own process.'),
      numeric(5, 'A 20 Hz control timer is starved by a callback that blocks for 300 ms. Approximately how many control periods are lost? [0-100]', 6, '300 ms at 20 Hz (50 ms per period) is about 6 missed periods. The control loop effectively stops for a third of a second - which for a moving robot is a large uncontrolled motion.', { tolerance: 1 }),
      short(5, 'Why must an action server always return a terminal result?', ['a client waiting forever is a hung system that cannot recover or escalate', 'otherwise the client blocks indefinitely', 'a missing result hangs the client'], 'A client awaiting a result cannot proceed, retry or escalate, so a missing terminal result becomes a hung system rather than a failed task. Every path - success, cancellation, each failure mode - must end in a result with a distinct status the caller can act on.'),
    ],
    skills: ['ros2-services', 'ros2-actions', 'executor-threading', 'callback-groups'],
  }),

  lesson({
    id: 'ros-04',
    subject: 'ros2',
    order: 4,
    title: 'TF2 and Time: The Transform Tree Done Right',
    difficulty: 'advanced',
    minutes: 55,
    prereqs: ['rob-02', 'ros-02'],
    description: 'The transform tree, static versus dynamic transforms, timestamped lookups and interpolation, and the extrapolation and frame errors that make TF2 the most common source of confusing robot bugs.',
    why: 'Every spatial question a robot answers goes through TF2: where is that obstacle relative to me, where is the camera relative to the arm, where was I when this scan was taken. Misusing it produces numbers that look reasonable and are wrong - the hardest class of bug to find.',
    objectives: [
      'Structure a transform tree with map, odom, base and sensor frames and explain each frame\'s role',
      'Publish static and dynamic transforms correctly, with exactly one parent per frame',
      'Look up a transform at a specific timestamp and explain why interpolation matters',
      'Diagnose extrapolation, missing-frame and multiple-parent TF2 errors',
      'Verify a transform chain against a physical measurement',
    ],
    learn: [
      {
        kind: 'table',
        heading: 'The standard frame tree (REP-105)',
        columns: ['Frame', 'Meaning', 'Properties'],
        rows: [
          ['earth / map', 'A fixed, world-anchored frame from a map or a global fix', 'No discontinuities within a session, but the estimate can jump when localization corrects'],
          ['odom', 'Continuous, drift-free-locally frame anchored where the robot started', 'Smooth and locally accurate, globally drifting - never jumps'],
          ['base_link', 'Rigidly attached to the robot body, origin at the drive axle centre, x forward, z up', 'The frame all body-mounted sensors are calibrated against'],
          ['base_scan / camera_link', 'Sensor frames, static offsets from base_link', 'Static unless the sensor moves on a joint'],
          ['Joint frames (link_1, link_2, ...)', 'Published by the robot state publisher from URDF and joint states', 'Dynamic - they move with the joints'],
        ],
      },
      {
        kind: 'formula',
        heading: 'The map->odom trick, which is the heart of localization',
        formula: 'T_map_base = T_map_odom * T_odom_base     where T_odom_base comes from odometry (smooth, drifting) and T_map_odom is the localization correction (occasionally discontinuous)',
        defines: ['T_map_odom = published by the localization node', 'T_odom_base = published by the odometry node'],
        body: [
          'Odometry is smooth but drifts. Localization against a map is accurate but produces occasional jumps when it corrects. The tree separates them: the odom->base_link branch stays continuous for anything that needs smooth motion (controllers, local planners), and map->odom carries the correction for anything that needs global accuracy (global planner, mission logic).',
          'This is why a controller should use odom, not map. If it uses map, a localization correction injects a step into its feedback and the robot lurches. That single design decision prevents a whole class of instability.',
          'And it is why every frame must have exactly one parent: a tree, not a graph. Two parents for one frame means two contradictory answers to the same question.',
        ],
      },
      {
        kind: 'callout',
        tone: 'warning',
        heading: 'The three TF2 errors you will actually see',
        body: [
          'EXTRAPOLATION INTO THE FUTURE: "transform at time T is not available, latest is T-x". You asked for a transform newer than anything published - usually because you stamped data with the wrong time, or the publisher is slower than the consumer, or you used "now" on a node whose clock differs from the publisher\'s. Fix: look up at the sensor timestamp, not at now; check clock sources; add a small tolerance.',
          'EXTRAPOLATION INTO THE PAST: the transform has been dropped from the buffer because it is older than the cache (default 10 s). Fix: publish dynamic transforms continuously, increase the buffer, or process data promptly rather than after a long queue.',
          'FRAME NOT FOUND / NO CHAIN: a frame_id is misspelled, a static transform was never published, or the tree is disconnected. Fix: `ros2 run tf2_tools view_frames` generates a PDF of the actual tree - compare it with the tree you intended. It finds this class of bug in seconds.',
          'Bonus fourth: a static transform published repeatedly with slightly different values, or published on /tf instead of /tf_static, which quietly makes the tree jitter.',
        ],
      },
      {
        kind: 'text',
        heading: 'Timestamps, interpolation and the cost of getting it wrong',
        body: [
          'TF2 keeps a time-buffered history of every transform and interpolates to answer "where was frame B relative to frame A at time T". That is exactly what you need when a sensor measurement took 40 ms to acquire and the robot moved during it.',
          'So look up transforms at the message timestamp: lookup_transform(target, source, msg.header.stamp). Using rclpy.time.Time() (meaning "latest available") is convenient and wrong for anything moving - it silently applies a transform from a different moment.',
          'Static transforms (sensor mounts, fixed offsets) should be published once on /tf_static with a zero or timeless stamp, and they must come from a measured calibration, not from an eyeballed CAD number. A 2-degree error in a camera mount becomes centimetres of error at 2 m - which is how a robot confidently grasps at empty air.',
        ],
      },
      {
        kind: 'example',
        heading: 'Worked example - how much does a stale transform cost?',
        problem: 'A robot moves at 0.6 m/s and rotates at 0.5 rad/s. A lidar scan takes 25 ms to acquire and 30 ms to process before it is used. If you apply the latest transform instead of the transform at the scan timestamp, how large is the position error of a reported obstacle at 4 m range, 45 degrees off the heading?',
        solution: [
          'Total elapsed time from measurement to use = 25 + 30 = 55 ms = 0.055 s',
          'Translation error = v * t = 0.6 * 0.055 = 0.033 m = 33 mm',
          'Rotation error = omega * t = 0.5 * 0.055 = 0.0275 rad = 1.58 degrees',
          'At 4 m range, a 1.58-degree angular error displaces the obstacle by 4 * sin(1.58 deg) = 4 * 0.0276 = 0.110 m = 110 mm laterally',
          'Combined, the reported obstacle position is wrong by roughly 115 mm - dominated by the rotation term, not the translation',
          'Conclusion: angular latency matters more than linear latency at range. At 10 m the same 1.58 degrees becomes 275 mm of error',
          'Fix: stamp the scan at acquisition time and look up the transform at that stamp; TF2 will interpolate the tree correctly and the error collapses to the interpolation error, which is millimetres',
        ],
        answer: 'About 115 mm of error at 4 m, dominated by the 1.58-degree rotation applied at range. Timestamped lookup removes nearly all of it - which is why "use the latest transform" is a bug, not a shortcut.',
      },
    ],
    resources: [
      R('ROS 2 documentation: tf2 concepts and tutorials', 'docs', 'https://docs.ros.org/en/rolling/Tutorials/Intermediate/Tf2/Tf2-Main.html', { author: 'Open Robotics', minutes: 120, note: 'Free. Includes writing a broadcaster and listener, and the time-travel and interpolation examples.' }),
      R('ROS REP-105: coordinate frames for mobile platforms', 'docs', 'https://www.ros.org/reps/rep-0105.html', { author: 'Open Robotics', minutes: 20, note: 'Defines map, odom, base_link and their required properties. Short, and the reference the whole ecosystem follows.' }),
      R('ROS REP-103: units and axis conventions', 'docs', 'https://www.ros.org/reps/rep-0103.html', { author: 'Open Robotics', minutes: 20, note: 'Metres, radians, x forward, y left, z up. Adopt it verbatim.' }),
      R('ROS 2 tf2_tools: view_frames', 'docs', 'https://docs.ros.org/en/rolling/p/tf2_tools/', { author: 'Open Robotics', minutes: 15, note: 'Generates a PDF of the live transform tree - the fastest way to find a disconnected or wrong-parent frame.' }),
    ],
    exercises: [
      X('build', 'Publish a complete transform tree for your robot: base_link, a statically calibrated lidar mount, a camera mount, and odom from wheel odometry. Generate the tree PDF with view_frames and check it matches your intended design.', 60, { solution: 'The tree must be connected with exactly one parent per frame, static transforms on /tf_static, and odom->base_link published continuously from odometry. view_frames will show any disconnected frame immediately - fix the tree before touching any consumer.' }),
      X('measurement', 'Calibrate a sensor mount statically: measure the offset from base_link with a ruler or from CAD, then verify it by placing an object at a known position and comparing the transformed reading. Report the residual error in millimetres and degrees.', 50, { solution: 'Expect 5-30 mm and 0.5-2 degrees of error from measurement and mounting. Quantify it, because that number is the floor on everything downstream - a navigation stack cannot be more accurate than its calibration.' }),
      X('code', 'Write a node that looks up a transform at a message timestamp with a tolerance, handles the three TF2 exceptions explicitly (future extrapolation, past extrapolation, no chain), and logs which one occurred with the timestamps involved. Prove each path by injecting a late or badly stamped message.', 60, { solution: 'Each exception needs a distinct response: retry with a small timeout for future extrapolation, drop and report for past extrapolation, and alarm for a missing chain (which is a configuration bug, not a transient). Logging the requested stamp and the latest available stamp is what makes the failure diagnosable.' }),
    ],
    questions: [
      mcq(1, 'Why do both map and odom frames exist?', ['Odom is smooth but drifts; map is globally accurate but can jump - separating them lets controllers use odom and global logic use map', 'They are redundant', 'map is for simulation only', 'odom is only for wheel encoders'], 0, 'The split is deliberate: T_map_odom carries the localization correction, so a correction never injects a step into a controller that reads odom.'),
      mcq(1, 'Which frame should a velocity controller use for its feedback?', ['odom, because it is continuous and never jumps', 'map, because it is accurate', 'earth', 'base_scan'], 0, 'A localization correction in map would appear as a step in the controller\'s feedback and cause a lurch. Global accuracy is the planner\'s problem, not the controller\'s.'),
      mcq(2, 'A transform tree must have:', ['Exactly one parent per frame - it is a tree, not a graph', 'Two parents per frame for redundancy', 'No static transforms', 'A cycle through odom'], 0, 'Two parents would give two contradictory answers for the same relative pose. TF2 rejects or warns about such frames because the result is undefined.'),
      mcq(2, 'Static transforms should be published:', ['Once, on /tf_static, from a measured calibration', 'Continuously on /tf at 50 Hz', 'Only when requested', 'With the current timestamp each cycle'], 0, 'Continuous republication of static data wastes bandwidth and can make the tree jitter if values differ slightly. Static means static - and it must be measured, not guessed.'),
      mcq(3, 'Why look up a transform at the message timestamp instead of "now"?', ['Because the robot moved between measurement and use, so "now" applies a transform from the wrong moment', 'It is faster', 'Now is not supported', 'Timestamps are optional'], 0, 'The error scales with speed and with range: at 4 m a 55 ms latency gave about 115 mm of error in the worked example, dominated by rotation.'),
      mcq(3, '"Extrapolation into the future" usually means:', ['You requested a transform newer than the latest published one - a stamp, clock or rate problem', 'The robot is moving too fast', 'The buffer is too large', 'QoS is incompatible'], 0, 'Causes: stamping data with publish time, mismatched clocks, a publisher slower than the consumer, or asking for now on a different clock. Log the requested and latest stamps to see it.'),
      mcq(3, 'A transform lookup fails because the data is older than the buffer. What is the fix?', ['Process data promptly rather than after a long queue, or increase the buffer; ensure dynamic transforms are published continuously', 'Use the latest transform instead', 'Ignore the timestamp', 'Republish as static'], 0, 'The default cache is about 10 s. Falling back to the latest transform hides the problem and returns a wrong answer - never do that silently.'),
      mcq(4, 'Which tool tells you the tree that actually exists at runtime?', ['tf2_tools view_frames, which renders the live tree to a PDF', 'ros2 topic hz', 'ros2 param get', 'rosbag info'], 0, 'Compare the rendered tree with your intended design. It finds disconnected frames, wrong parents and missing static transforms in seconds.'),
      mcq(4, 'A camera mount has a 2-degree yaw error in its static transform. At 3 m range, how large is the resulting position error of a detected object, roughly?', ['About 10 cm, because angular calibration error scales with range', 'About 2 mm', 'Zero - it cancels out', 'About 3 m'], 0, '3 m * sin(2 deg) = 0.105 m. This is why static transforms come from measurement, and why calibration quality bounds all downstream accuracy.'),
      numeric(4, 'A robot rotates at 0.8 rad/s and a scan is used 40 ms after acquisition. What angular error in degrees results from using the latest transform? [0-90]', 1.83, '0.8 * 0.040 = 0.032 rad = 1.83 degrees. At 5 m that is about 160 mm of lateral error on every reported obstacle.', { tolerance: 0.05 }),
      numeric(5, 'The same robot translates at 0.5 m/s. What position error in millimetres does that 40 ms produce from translation alone? [0-1000]', 20, '0.5 * 0.040 = 0.020 m = 20 mm - smaller than the rotational term at range, which is the usual ordering.', { tolerance: 1 }),
      short(5, 'Why does a localization correction in the map frame never reach a controller that reads odom?', ['the correction is isolated in T_map_odom, leaving the odom branch continuous', 'it lives in the map to odom transform only', 'the odom branch stays smooth'], 'The correction is published as T_map_odom, so the odom->base_link branch remains a pure, continuous odometry integration. Consumers choose their branch: controllers read odom for smoothness, global planners read map for accuracy. That separation is the design, not an accident.'),
    ],
    skills: ['tf2-frames', 'transform-lookups', 'tf2-debugging', 'sensor-calibration'],
  }),

  lesson({
    id: 'ros-05',
    subject: 'ros2',
    order: 5,
    title: 'Parameters, Lifecycle, Launch and Real-Time Discipline',
    difficulty: 'advanced',
    minutes: 60,
    prereqs: ['ros-03', 'emb-02'],
    description: 'Configuring nodes with declared, typed parameters; managed lifecycle nodes with explicit state transitions; launch files that compose a system; and the discipline needed to keep a control loop deterministic inside a middleware.',
    why: 'This is the layer that turns a set of working nodes into a system that starts reliably, can be reconfigured safely, and does not miss deadlines. Parameters that are not declared, nodes that start in any order, and control loops that allocate memory are the three ways robot software fails in the field.',
    objectives: [
      'Declare, type and validate parameters, and handle changes at runtime safely',
      'Use lifecycle nodes to control startup ordering and reject work in the wrong state',
      'Compose a system with launch files, arguments and namespaces',
      'Identify the sources of non-determinism in a middleware-based control loop',
      'Apply concrete measures to bound jitter in a real-time-relevant node',
    ],
    learn: [
      {
        kind: 'text',
        heading: 'Parameters: declared, typed, validated',
        body: [
          'In ROS 2 parameters must be DECLARED with a type and a default before use. That is not bureaucracy: it catches typos at startup, makes the configuration discoverable (`ros2 param list`, `ros2 param dump`), and prevents a string where a double was expected.',
          'Declare every parameter your node reads, with a default that is safe rather than convenient. A node that crashes on a missing parameter is better than one that silently runs with an uninitialised gain - but a node that starts with a safe default and logs a warning is better than both.',
          'Handle runtime changes deliberately. Some parameters can change safely (a logging level, a target); others cannot (a control gain mid-motion, a wheel diameter while odometry is integrating). Register a callback that validates and rejects unsafe changes, and never read a parameter inside a hot loop without a cached, atomically-updated copy.',
        ],
      },
      {
        kind: 'table',
        heading: 'Lifecycle states and what they are for',
        columns: ['State', 'Meaning', 'What you do there'],
        rows: [
          ['unconfigured', 'Constructed, no resources held', 'Nothing - declare parameters only'],
          ['inactive', 'Configured, resources allocated, not publishing', 'Open devices, allocate buffers, load maps, validate configuration'],
          ['active', 'Doing its job, publishing', 'Run the loops; reject reconfiguration that would break motion'],
          ['finalized', 'Shutting down cleanly', 'Stop motion, close devices, flush logs'],
        ],
      },
      {
        kind: 'callout',
        tone: 'note',
        heading: 'Why lifecycle nodes solve the startup race',
        body: [
          'With discovery-based startup, a controller may become active before the driver that publishes odometry exists. The controller then acts on missing data - or worse, on stale data - during the first seconds of every run.',
          'Managed (lifecycle) nodes let a supervisor bring the system up in a defined order: configure all nodes, then activate drivers, then estimators, then planners, then controllers. Each transition is a checkpoint where a node can refuse to activate because its inputs are not present.',
          'The rule to encode in on_activate: verify that required inputs have been received recently and are valid, otherwise return FAILURE and stay inactive. A robot that refuses to start is a robot you can debug; one that starts broken moves unpredictably.',
        ],
      },
      {
        kind: 'text',
        heading: 'Real-time discipline inside a middleware',
        body: [
          'ROS 2 can be used for real-time control, but not by default. Non-determinism comes from: dynamic memory allocation in callbacks, DDS discovery traffic, garbage collection (in Python nodes especially), the general-purpose OS scheduler, logging, and mutex contention between callbacks.',
          'Concrete measures, in order of payoff: keep the hot loop allocation-free (preallocate buffers, avoid creating messages per cycle); do not log inside the loop except on state change; pin the control thread to an isolated core with a real-time priority; keep the control node in its own process with a dedicated executor; use intra-process communication or shared memory for large messages within a machine; and measure jitter rather than assuming it.',
          'Language choice matters. A 1 kHz loop with hard timing belongs in C++ with allocation discipline; Python is excellent for planning, perception and tooling but its garbage collector makes tight jitter hard to guarantee.',
          'And be honest about the platform: on a general-purpose Linux kernel you will get good average latency and occasional spikes. If your application cannot tolerate a spike, you need a real-time kernel or the loop must run on a microcontroller - which is exactly why the motor cascades live in firmware.',
        ],
      },
      {
        kind: 'example',
        heading: 'Worked example - measuring and fixing jitter',
        problem: 'A 200 Hz control node on a shared Linux board shows a mean period of 5.0 ms but occasional periods of 40 ms. Diagnose the likely causes in order and state how you would confirm each.',
        solution: [
          'First, measure properly: log the actual callback entry timestamps and compute the distribution (mean, p99, max) rather than looking at averages. A 40 ms spike at p99.9 is a different problem from one every second',
          'Cause 1 - allocation or logging in the loop: inspect the callback for message construction, string formatting or log calls. Confirm by removing them and re-measuring',
          'Cause 2 - other callbacks on the same executor: a 40 ms stall smells like a serialised slow callback. Confirm by moving the control node to its own process with a dedicated single-threaded executor',
          'Cause 3 - OS scheduling: the board is running other work (a planner, a bag recorder, wifi). Confirm with a scheduler trace or by stopping other processes one at a time',
          'Cause 4 - discovery or QoS traffic bursts: DDS discovery on a busy network causes periodic stalls. Confirm by timing the spikes against network activity',
          'Fixes in order of payoff: isolate the process, preallocate, remove logging, pin to a core with real-time priority, then consider a real-time kernel or moving the loop to the microcontroller',
          'Acceptance criterion, written down before fixing: p99 period within 20% of nominal (6 ms), maximum below 15 ms, sustained for ten minutes under full system load',
        ],
        answer: 'Measure the distribution first, then eliminate in order: in-loop allocation and logging, executor contention, OS scheduling, DDS traffic. Isolate the process and preallocate before reaching for a real-time kernel - and define the jitter acceptance criterion up front.',
      },
    ],
    resources: [
      R('ROS 2 documentation: about parameters', 'docs', 'https://docs.ros.org/en/rolling/Concepts/Basic/About-Parameters.html', { author: 'Open Robotics', minutes: 30 }),
      R('ROS 2 documentation: about lifecycle nodes (managed nodes)', 'docs', 'https://design.ros2.org/articles/node_lifecycle.html', { author: 'Open Robotics', minutes: 40, note: 'The design article explaining the states, transitions and why managed startup exists.' }),
      R('ROS 2 documentation: launch system', 'docs', 'https://docs.ros.org/en/rolling/Tutorials/Intermediate/Launch/Launch-Main.html', { author: 'Open Robotics', minutes: 90, note: 'Free. Launch files, arguments, namespaces, event handlers and composition.' }),
      R('Wikipedia: Real-time computing and determinism', 'article', 'https://en.wikipedia.org/wiki/Real-time_computing', { minutes: 30, note: 'Hard versus soft real time, jitter, and what a general-purpose OS can and cannot promise.' }),
    ],
    exercises: [
      X('code', 'Write a node that declares every parameter it uses with a type and a safe default, validates a runtime parameter change (rejecting an unsafe new value), and logs any parameter left at its default on startup.', 50, { solution: 'The three behaviours to demonstrate: declaration with type (so a typo fails fast), a change callback that rejects invalid values with a reason, and a startup report of defaults in use - which is how you catch a robot running with untuned gains.' }),
      X('code', 'Convert a node into a managed lifecycle node with on_configure allocating resources and on_activate verifying that its required inputs have arrived recently, refusing to activate otherwise. Demonstrate both the successful and the refused activation.', 60, { solution: 'on_activate must check input freshness (time since last message on each required topic) and return FAILURE when stale. Demonstrate by starting the node before its publisher exists: it should refuse, log which input is missing, and activate once data flows.' }),
      X('measurement', 'Measure the timing distribution of a periodic callback: log entry timestamps for two minutes at 100 Hz, then report mean, standard deviation, p95, p99 and maximum period. Apply one fix (isolate the process, preallocate, or remove logging) and re-measure.', 55, { solution: 'Report before-and-after distributions, not just averages. A typical result: mean unchanged, p99 improved several-fold after removing per-cycle allocation or logging. State the acceptance criterion you used and whether it was met.' }),
    ],
    questions: [
      mcq(1, 'Why must parameters be declared in ROS 2?', ['So a missing or wrongly typed parameter is caught at startup instead of silently misbehaving at runtime', 'For speed', 'Because DDS requires it', 'To allow undeclared access later'], 0, 'Declaration gives type checking, discoverability and safe defaults. An undeclared parameter read is a bug waiting for a typo.'),
      mcq(1, 'Which default value should a parameter have?', ['A safe value, even if it is not the tuned one - so a misconfigured robot does something benign', 'The tuned value from the last robot', 'Zero always', 'No default, so it must be set'], 0, 'Safety first: a safe default plus a startup warning beats a crash, and beats silently running with a dangerous value. Log which parameters are still at defaults.'),
      mcq(1, 'Which parameter change is unsafe to accept at runtime?', ['A wheel diameter or control gain while odometry is integrating or the robot is moving', 'A logging level', 'A target waypoint', 'A diagnostic threshold'], 0, 'Calibration and gains define the model the running loops depend on. Changing them mid-motion injects a discontinuity - reject and require a stopped, reconfigured state.'),
      mcq(2, 'What does the inactive lifecycle state mean?', ['Configured with resources allocated, but not publishing or acting', 'Crashed', 'Waiting for parameters', 'Running at reduced rate'], 0, 'Inactive is where you open devices, allocate buffers and load maps. Activation then becomes a fast, checkable transition rather than a slow startup.'),
      mcq(2, 'What should on_activate verify before succeeding?', ['That required inputs have been received recently and are valid', 'That the node has parameters', 'That the clock is noon', 'Nothing - activation always succeeds'], 0, 'Refusing to activate with missing inputs turns a silent, moving-while-broken failure into a debuggable startup error. Return FAILURE and stay inactive.'),
      mcq(3, 'Launch files are primarily for:', ['Composing a system: starting nodes with arguments, namespaces, remappings and conditional logic', 'Compiling code', 'Defining message types', 'Recording bags'], 0, 'A launch file is the system definition. Namespaces and remappings let the same nodes be reused for multiple robots or for simulation and hardware.'),
      mcq(3, 'Why use namespaces and remappings rather than hardcoded topic names?', ['So the same nodes can run for different robots, or against simulation versus hardware, without editing code', 'It is faster', 'It is required by DDS', 'It reduces bandwidth'], 0, 'Hardcoded names make every environment change a code change and every multi-robot setup a fork. Namespaces are configuration; keep behaviour in code.'),
      mcq(4, 'Which is NOT a typical source of non-determinism in a ROS 2 control loop?', ['Static transforms published on /tf_static', 'Dynamic memory allocation inside the callback', 'Other callbacks serialised on the same executor', 'OS scheduling and other processes on the board'], 0, 'Static transforms are published once and are irrelevant to loop timing. Allocation, executor contention, logging, GC and scheduling are the real sources.'),
      mcq(4, 'Why does a 1 kHz hard-real-time loop belong on a microcontroller rather than in a Linux node?', ['A general-purpose OS gives good average latency but occasional spikes that a hard deadline cannot tolerate', 'Linux cannot run at 1 kHz', 'Microcontrollers have more memory', 'ROS 2 does not support 1 kHz timers'], 0, 'Soft real time with spikes is fine for 50 Hz navigation; a current loop needs bounded worst case. That is why the fast cascades live in firmware and the middleware runs above them.'),
      mcq(5, 'What is the first step when a control loop shows occasional 40 ms periods?', ['Measure the full distribution of callback periods (mean, p95, p99, max) before changing anything', 'Increase the loop rate', 'Rewrite the node in C++', 'Buy a faster board'], 0, 'Averages hide spikes and spikes hide causes. Get the distribution, then eliminate causes in order: in-loop allocation and logging, executor contention, scheduling, DDS traffic.'),
      numeric(5, 'A 100 Hz loop has a nominal period of 10 ms and a measured p99 of 13 ms. What is the p99 jitter as a percentage of nominal? [0-200]', 30, '(13 - 10)/10 = 30% jitter at p99. Whether that is acceptable depends on the loop: a navigation loop tolerates it, a velocity loop probably does not.', { tolerance: 1 }),
      short(5, 'Why should a written jitter acceptance criterion exist before you start optimising?', ['it defines done and prevents optimising the wrong tail of the distribution', 'without it you cannot tell when the work is finished', 'it targets the right percentile'], 'Because "less jitter" is unfalsifiable. A criterion such as p99 within 20% of nominal and max below 15 ms under full load tells you which tail matters, when to stop, and whether a change helped - and it becomes a regression test you can re-run on any board.'),
    ],
    skills: ['ros2-parameters', 'lifecycle-nodes', 'launch-composition', 'realtime-jitter'],
  }),

  lesson({
    id: 'ros-06',
    subject: 'ros2',
    order: 6,
    title: 'Debugging, Recording and Testing a Robot System',
    difficulty: 'advanced',
    minutes: 55,
    prereqs: ['ros-05', 'tool-05'],
    description: 'The practical toolkit: CLI introspection, bag recording and deterministic replay, plotting and visualisation, structured logging, and how to build automated tests for a system whose inputs are physical.',
    why: 'A robot failure is rarely reproducible on demand, and the hardware is often not next to your desk. The ability to record a run, replay it, and reproduce the failure offline is the difference between debugging by measurement and debugging by guesswork - and it is a skill you must set up before you need it.',
    objectives: [
      'Use the ROS 2 CLI to inspect a live graph: nodes, topics, rates, bandwidth and services',
      'Record and replay bags so a failure can be reproduced offline',
      'Plot and visualise signals to find a fault rather than reading numbers',
      'Structure logging so a field failure can be reconstructed afterwards',
      'Design automated tests for robot software, including what can and cannot be tested offline',
    ],
    learn: [
      {
        kind: 'table',
        heading: 'The CLI commands that solve most problems',
        columns: ['Command', 'Answers'],
        rows: [
          ['ros2 node list / ros2 node info <node>', 'What exists, and what each node publishes, subscribes to and serves'],
          ['ros2 topic list -t', 'Every topic with its message type - the map of your data flow'],
          ['ros2 topic echo <topic>', 'The actual content arriving, which settles any argument about what a node sends'],
          ['ros2 topic hz / bw <topic>', 'Measured rate and bandwidth - the first thing to check when a loop seems slow'],
          ['ros2 topic info <topic> --verbose', 'Endpoint counts and QoS of each - the QoS-mismatch diagnostic'],
          ['ros2 service list / call', 'What services exist and what they return when invoked manually'],
          ['ros2 param list / get / dump', 'The live configuration - dump it into the bug report'],
          ['ros2 bag record / info / play', 'Capture a run, inspect it, replay it offline'],
          ['tf2_tools view_frames / tf2_echo', 'The actual transform tree and a specific transform over time'],
        ],
      },
      {
        kind: 'callout',
        tone: 'note',
        heading: 'Record first, diagnose second',
        body: [
          'Before your first hardware run, decide what you will record and set it up: all sensor topics, all command topics, /tf and /tf_static, odometry, diagnostics and the parameter dump. Disk is cheap; a failure you cannot reproduce is expensive.',
          'Be selective about high-bandwidth topics. Recording raw camera video at 30 fps fills a card in minutes and makes the bag unwieldy - record the compressed stream, or the detections, and only record raw video when the camera itself is under investigation.',
          'Then replay offline against the nodes you are debugging: `ros2 bag play` with the clock published gives you a deterministic, repeatable test bench. A bug you can reproduce in ten seconds on your desk will be fixed; one that needs a physical robot and an afternoon will not.',
          'One caution: replaying a bag into a live robot is dangerous. Commands in the bag will be executed. Replay into a system with the actuators disconnected or simulated, never into a machine that can move.',
        ],
      },
      {
        kind: 'text',
        heading: 'Logging that reconstructs a failure',
        body: [
          'Log at levels with discipline: DEBUG for per-cycle values (off in the field), INFO for state transitions and mode changes, WARN for anything the system recovered from, ERROR for anything that degraded behaviour, FATAL for unsafe conditions. A log where everything is INFO is a log nobody can read.',
          'Log transitions, not samples. "Controller: ACTIVE, gains Kp=2.0 Ki=0.5" once beats printing the gains every cycle. "Localization confidence fell below 0.7" beats a stream of confidence values.',
          'Include the context that makes a line useful: which node, which state, the relevant timestamps and the values that decided the branch taken. And never log inside a hot loop - it is both a jitter source and a disk-filler.',
          'Diagnostics are the structured version of this: hardware level, error codes, temperatures, voltages and watchdog states published as messages, so they can be recorded, plotted and alarmed on rather than read from a terminal after the fact.',
        ],
      },
      {
        kind: 'table',
        heading: 'What can and cannot be tested offline',
        columns: ['Testable offline (do automate these)', 'Requires hardware or simulation'],
        rows: [
          ['Message and transform handling: parsing, stamping, frame conversion', 'Actuator response, friction, backlash, thermal behaviour'],
          ['Planners and algorithms on recorded bags', 'Sensor noise, dropout, reflections and real lighting'],
          ['State machines and failure handling with injected faults', 'Contact, grip, terrain and slip'],
          ['Parameter validation and lifecycle transitions', 'End-to-end timing under real CPU and network load'],
          ['Regression tests on known datasets with expected outputs', 'Anything certified as "works" for a user'],
        ],
      },
      {
        kind: 'example',
        heading: 'Worked example - debugging a robot that stops mid-corridor',
        problem: 'A robot navigates correctly for about 20 seconds, then stops in a corridor and will not resume. There is no error in the terminal. Describe the diagnostic sequence.',
        solution: [
          'Step 1 - reproduce with data: record a bag of all sensor, command, tf and diagnostic topics during a failing run. Without this you are guessing',
          'Step 2 - replay offline and watch the command path: is /cmd_vel still being published when the robot stops? If yes, the fault is downstream of the software (motor driver, safety stop, current limit). If no, the fault is in the stack',
          'Step 3 - if /cmd_vel stopped, find who stopped publishing it: check the controller node is alive (`ros2 node list`), then its inputs - is /scan arriving (`ros2 topic hz /scan`), is /tf valid (`tf2_echo map base_link`), is localization publishing?',
          'Step 4 - a corridor suggests two classic causes: a narrow passage where the inflation radius leaves no valid path (planner reports "no path" or the costmap is fully inflated), or lidar degradation - long smooth walls and specular reflections giving invalid returns',
          'Step 5 - inspect the costmap in the recorded data at the failure moment: if the corridor is fully occupied, the inflation radius or the sensor data is wrong. Compare against the measured corridor width',
          'Step 6 - check the failure was actually reported: a well-designed system logs "planner: no valid path, corridor clearance 0.05 m below inflation 0.25 m". If nothing was logged, that is the second bug - the system failed silently',
          'Fixes: reduce inflation or use a cost that allows tight passages with caution, filter invalid lidar returns explicitly, and make "no path" a reported, recoverable state with a defined behaviour (back up, replan, ask for help)',
        ],
        answer: 'Record, replay, follow the command path from the top down, and inspect the costmap and sensor data at the failure instant. The corridor case is usually inflation versus clearance, or invalid lidar returns on smooth walls - and a silent failure is itself a bug worth fixing.',
      },
    ],
    resources: [
      R('ROS 2 documentation: rosbag2 recording and playback', 'docs', 'https://docs.ros.org/en/rolling/Tutorials/Beginner-CLI-Tools/Recording-And-Playing-Back-Data/Recording-And-Playing-Back-Data.html', { author: 'Open Robotics', minutes: 40, note: 'Free. Record, inspect, replay, and filter topics - the core of offline debugging.' }),
      R('ROS 2 documentation: CLI tools and demos', 'docs', 'https://docs.ros.org/en/rolling/Tutorials/Beginner-CLI-Tools.html', { author: 'Open Robotics', minutes: 60 }),
      R('PlotJuggler: time-series visualisation for ROS', 'tool', 'https://github.com/facontidavide/PlotJuggler', { author: 'Davide Faconti', minutes: 60, note: 'Free and open source. Plotting several signals against each other finds faults that scrolling numbers never will.' }),
      R('ROS 2 documentation: testing (launch_testing, unit and integration)', 'docs', 'https://docs.ros.org/en/rolling/Concepts/Intermediate/About-Testing.html', { author: 'Open Robotics', minutes: 45 }),
    ],
    exercises: [
      X('build', 'Set up a standard recording profile for your robot: a bag configuration listing every topic you will record with a justification, plus a parameter dump and a log of the software version. Run it during a normal session and report the resulting bag size and duration.', 45, { solution: 'Justify each topic by what failure it would let you diagnose. Exclude or compress high-bandwidth topics: recording raw video typically dominates the size. Report MB per minute so you can plan storage for longer runs.' }),
      X('code', 'Write an offline regression test that replays a recorded bag into a node under test and asserts on its output - for example that the planner produces a collision-free path, or that the controller keeps following error below a threshold. Include one injected fault case.', 60, { solution: 'The test must be deterministic: publish the clock from the bag, wait for the node to be ready, and compare against recorded expectations with a tolerance. The injected-fault case (a gap in the scan, a stale transform) proves the node fails safely rather than silently.' }),
      X('question', 'A robot behaves badly in the field and you have no recording. List the five things you will change in the software so that next time the failure is diagnosable, and state what each one would reveal.', 35, { solution: '(1) Record all sensor, command, tf and diagnostic topics - reveals the actual data. (2) Structured diagnostics with hardware level, current, temperature, watchdog state - reveals hardware faults. (3) Log every state transition with the values that caused it - reveals decision errors. (4) Log recovered warnings, not just failures - reveals marginal behaviour before it becomes a fault. (5) Record the parameter dump and software version at startup - reveals configuration drift between runs.' }),
    ],
    questions: [
      mcq(1, 'Which command answers "is this topic actually arriving, and how fast"?', ['ros2 topic hz', 'ros2 node list', 'ros2 param get', 'ros2 bag info'], 0, 'Measured rate on the receiving side. ros2 topic echo shows content, ros2 topic bw shows bandwidth - all three are the first things to check when a loop seems slow.'),
      mcq(1, 'What does `ros2 topic info <topic> --verbose` add over the plain form?', ['The QoS profile of every endpoint, which reveals incompatibilities', 'The message content', 'The publishing rate', 'The node graph'], 0, 'Endpoint counts plus QoS. Visible endpoints with zero delivered messages is a QoS mismatch until proven otherwise.'),
      mcq(2, 'Why record a bag before attempting to diagnose an intermittent robot failure?', ['Because the failure may not reproduce on demand, and offline replay turns it into a repeatable test bench', 'Bags improve performance', 'Recording is required by ROS 2', 'It replaces logging'], 0, 'A failure you can replay in ten seconds gets fixed. One that needs a physical robot and the right conditions usually does not.'),
      mcq(2, 'What is the danger of replaying a recorded bag into a live robot?', ['Recorded command topics will be executed, so the robot moves according to the old commands', 'The bag will be corrupted', 'Nothing - replay is read-only', 'The clock will desynchronise only'], 0, 'Replay publishes everything that was recorded, including /cmd_vel. Replay into a system with actuators disconnected or simulated - never into a machine that can move.'),
      mcq(2, 'Why exclude or compress raw camera video from a routine recording?', ['It dominates bag size and can make the recording unwieldy, while rarely being needed to diagnose a non-vision fault', 'It cannot be recorded', 'It breaks the clock', 'It is not a valid topic'], 0, 'Record the compressed stream or the detection output routinely, and raw video only when the camera itself is under investigation. Storage discipline is what makes recording habitual.'),
      mcq(3, 'Why is plotting several signals together better than reading logged numbers?', ['Because a fault usually shows as a relationship between signals - a command rising while feedback stays flat - which is invisible in text', 'Plots use less disk', 'Numbers cannot be logged', 'Plotting is faster to produce'], 0, 'Command versus feedback, current versus velocity, confidence versus time: the divergence is the diagnosis. Text logs are for reconstructing decisions afterwards.'),
      mcq(3, 'What belongs in an INFO-level log line?', ['State and mode transitions, with the values that caused them', 'Every control cycle output', 'Per-sample sensor values', 'Nothing - INFO should be unused'], 0, 'Log transitions, not samples. Per-cycle data belongs in recorded topics where it can be plotted, not in a text log that nobody can read.'),
      mcq(4, 'Which is testable offline from a recorded bag?', ['Planner behaviour, transform handling, state machines and failure paths with injected faults', 'Friction and backlash', 'Grip and contact', 'Motor thermal limits'], 0, 'Software logic and algorithms replay deterministically. Physical effects - friction, contact, thermal behaviour, real sensor noise - need hardware or a well-validated simulator.'),
      mcq(4, 'What makes a bag-replay regression test deterministic?', ['Publishing the clock from the bag, waiting for the node to be ready, and comparing outputs with a defined tolerance', 'Running it twice', 'Using real time', 'Increasing the playback rate'], 0, 'Sim time plus readiness synchronisation plus tolerances. Without them the test flakes, and a flaky test is worse than no test because it teaches you to ignore failures.'),
      mcq(5, 'A robot stops mid-corridor with no error logged. What is the second bug, beyond whatever caused the stop?', ['The system failed silently - a stop must be a reported, recoverable state with a defined behaviour', 'There is no second bug', 'The corridor is too narrow', 'Logging was disabled correctly'], 0, 'Silent failure is a design defect. "No valid path" should be logged, reported as a diagnostic, and trigger a defined recovery - back up, replan, or ask for help.'),
      numeric(5, 'A recording profile captures 60 kB/s of sensor and command data plus a compressed image stream at 1.5 MB/s. How many MB does a 10-minute run use? [0-5000]', 936, 'Image: 1.5 MB/s * 600 s = 900 MB. Other topics: 0.06 MB/s * 600 = 36 MB. Total about 936 MB - which is why the image stream dominates the storage decision.', { tolerance: 10 }),
      short(5, 'Why should a parameter dump and software version be recorded at the start of every run?', ['configuration drift between runs otherwise makes results incomparable and faults unreproducible', 'so runs are comparable and reproducible', 'it exposes configuration drift'], 'Because a behaviour difference between two runs is usually a configuration difference. With the parameter dump and version captured, you can prove what changed instead of guessing - and without them, an intermittent fault may be a different gain set on a different day.'),
    ],
    skills: ['ros2-cli-debugging', 'bag-recording', 'signal-plotting', 'robot-regression-tests'],
  }),
];
